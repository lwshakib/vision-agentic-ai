/**
 * Global Application Constants
 */

/**
 * The maximum number of tokens allowed in the model's output response.
 * This ensures responses don't exceed the context window or usage limits.
 */
export const MAXIMUM_OUTPUT_TOKENS = 65535;
